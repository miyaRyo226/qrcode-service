export interface Dictionary {
	title: string;
	ssid: string;
	password: string;
	ssidPlaceholder: string;
	passwordPlaceholder: string;
	clearButton: string;
	generateButton: string;
	alertMessage: string;
	lightMode: string;
	darkMode: string;
}
